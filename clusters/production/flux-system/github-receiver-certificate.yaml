apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flux-github-webhook-tls
  namespace: flux-system
spec:
  secretName: flux-github-webhook-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - gitops-webhook.primed.io
